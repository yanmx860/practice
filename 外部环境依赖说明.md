# 外部环境依赖说明

## 全局依赖（所有服务共用）

| 组件 | 版本 | 端口 | 用途 |
|------|------|------|------|
| JDK | 8+ | - | 编译运行 |
| Maven | 3.6+ | - | 构建管理 |
| Nacos | 2.x | 8848 | 服务注册 + 配置中心，命名空间 `practice` |
| MySQL | 8.0 | 3306 | 业务数据库 |
| Redis | 6.x+ | 6379 | 分布式锁 + 订单超时 ZSet |

## 各服务依赖清单

### auth-service（8001）

| 依赖 | 配置值 | 用途 |
|------|--------|------|
| Nacos | `localhost:8848`，命名空间 `practice` | 注册发现 + 配置管理 |
| MySQL | `practice_auth` 库 | 存储用户账号与权限 |

> 依赖数：2 个

### order-service（8002）

| 依赖 | 配置值 | 用途 |
|------|--------|------|
| Nacos | `localhost:8848`，命名空间 `practice` | 注册发现 + 配置管理 |
| MySQL | `practice_order` 库 | 存储订单、订单项、订单日志 |
| RabbitMQ | `localhost:5672`，guest/guest | 发送订单创建事件给库存服务 |
| Redis | `localhost:6379` | 分布式锁 + ZSet 订单超时队列 |
| Seata | TC `localhost:8091`，事务组 `practice_tx_group` | 全局分布式事务协调 |
| | Nacos 注册中心 | Seata 注册 + 配置 |
| XXL-Job | Admin `http://localhost:8080/xxl-job-admin` | 定时扫描超时订单 |
| | Executor 端口 `9998` | 任务执行器注册端口 |

> 依赖数：6 个（含 Nacos/MySQL/RabbitMQ/Redis/Seata/XXL-Job）

### inventory-service（8003）

| 依赖 | 配置值 | 用途 |
|------|--------|------|
| Nacos | `localhost:8848`，命名空间 `practice` | 注册发现 + 配置管理 |
| MySQL | `practice_inventory` 库 | 存储商品库存 |
| RabbitMQ | `localhost:5672`，guest/guest | 接收订单创建事件，异步扣库存 |
| Seata | TC `localhost:8091`，事务组 `practice_tx_group` | 全局分布式事务分支 |
| | Nacos 注册中心 | Seata 注册 + 配置 |

> 依赖数：4 个（含 Nacos/MySQL/RabbitMQ/Seata）

### search-service（8004）

| 依赖 | 配置值 | 用途 |
|------|--------|------|
| Nacos | `localhost:8848`，命名空间 `practice` | 注册发现 + 配置管理 |
| ElasticSearch | `http://localhost:9200` | 商品全文搜索 |
| | IK 分词器插件 | 中文分词（需手动安装到 ES plugins 目录） |
| Kafka | `localhost:9092`，消费者组 `search-group` | 接收商品同步消息，同步 ES 索引 |

> 依赖数：3 个（含 Nacos/ES/Kafka）

### export-service（8005）

| 依赖 | 配置值 | 用途 |
|------|--------|------|
| Nacos | `localhost:8848`，命名空间 `practice` | 注册发现 + 配置管理 |
| MySQL | `practice_order` 库（与 order-service 同库） | 读取订单数据导出 |

> 依赖数：2 个（含 Nacos/MySQL）
> 
> ⚠️ 注意：export-service 与 order-service 共用 `practice_order` 库，因为导出线程直接查订单表，不做跨服务 Feign 查询。

### report-service（8006）

| 依赖 | 配置值 | 用途 |
|------|--------|------|
| Nacos | `localhost:8848`，命名空间 `practice` | 注册发现 + 配置管理 |
| MySQL | `practice_report` 库 | 存储报表数据 |
| XXL-Job | Admin `http://localhost:8080/xxl-job-admin` | 分布式调度中心 |
| | Executor 端口 `9999` | 任务执行器注册端口 |

> 依赖数：3 个（含 Nacos/MySQL/XXL-Job）

---

## 启动顺序

所有中间件启动后，各服务按以下顺序启动：

```
中间件 → auth-service → inventory-service → order-service
                                                    ↓
                                          search-service / export-service / report-service
```

| 序号 | 组件 | 说明 |
|------|------|------|
| 0 | Nacos | 必须先于所有服务启动 |
| 1 | MySQL + RabbitMQ + Kafka + ES + Seata + XXL-Job | 中间件可并行启动 |
| 2 | auth-service | 最优先，其他服务依赖 JWT 登录 |
| 3 | inventory-service | 先于 order-service，避免 Feign 调用失败 |
| 4 | order-service | 核心业务，启动后即可处理订单 |
| 5 | search-service / export-service / report-service | 无先后依赖，可并行启动 |

---

## 端口总览

| 组件 | 端口 |
|------|------|
| Nacos | 8848 |
| MySQL | 3306 |
| Redis | 6379 |
| RabbitMQ | 5672（AMQP），15672（管理台） |
| Kafka | 9092 |
| ElasticSearch | 9200（HTTP），9300（Transport） |
| XXL-Job Admin | 8080 |
| Seata TC | 8091 |
| **auth-service** | **8001** |
| **order-service** | **8002** |
| **inventory-service** | **8003** |
| **search-service** | **8004** |
| **export-service** | **8005** |
| **report-service** | **8006** |

---

## 快速安装参考

```bash
# Nacos（standalone 模式）
sh startup.sh -m standalone
# 访问 http://localhost:8848/nacos，创建命名空间 practice

# MySQL 8.0
# 执行项目根目录 init.sql 初始化 3 个库和所有表

# RabbitMQ
rabbitmq-server  # 或 Docker: docker run -d -p 5672:5672 -p 15672:15672 rabbitmq:management

# Kafka（需先启动 ZooKeeper）
bin/zookeeper-server-start.sh config/zookeeper.properties
bin/kafka-server-start.sh config/server.properties

# ElasticSearch 7.x（需安装 ik 分词器）
bin/elasticsearch-plugin install https://github.com/medcl/elasticsearch-analysis-ik/releases/download/v7.17.0/elasticsearch-analysis-ik-7.17.0.zip
bin/elasticsearch

# XXL-Job Admin
# 下载源码，执行 xxl-job-admin 数据库初始化脚本，启动 admin 服务

# Seata TC 1.6.1
# 下载 seata-server，配置 registry.conf 注册到 Nacos，启动
sh bin/seata-server.sh -p 8091
```
